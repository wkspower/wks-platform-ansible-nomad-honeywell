data_dir  = "/opt/nomad"
bind_addr = "0.0.0.0"

server {
  enabled          = true
  bootstrap_expect = 1
}

advertise {
  http = "{{ master_address }}"
  rpc  = "{{ master_address }}"
  serf = "{{ master_address }}"
}
